# WebSocket client library (Python 3.7 compatible)
websockets>=8.0,<11.0

# Audio processing with NumPy (Python 3.7 compatible)
numpy>=1.16.0,<1.22.0

# No sounddevice dependency - using ALSA directly via arecord subprocess